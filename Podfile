# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Flappy Hillary' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Flappy Hillary
  pod 'Google-Mobile-Ads-SDK'
  pod 'Firebase'
  pod 'Alamofire'

  target 'Flappy HillaryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Flappy HillaryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
